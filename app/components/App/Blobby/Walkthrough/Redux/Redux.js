import React from 'react';
import { connect } from 'react-redux';

class Redux extends React.Component {
    render() {
        return (
            <div>Redux section...</div>
        );
    }
}

export default Redux;
